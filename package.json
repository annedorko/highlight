{
    "scripts": {
        "install-pip": "pip install -r requirements.txt",
        "install-pipenv": "pipenv install",
        "install-conda": "conda install --file requirements.txt",
        "build": "python3 main.py build",
        "develop": "python3 main.py server --watch",
        "server": "python3 main.py server"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.16",
        "postcss-cli": "^10.1.0",
        "postcss-import": "^15.1.0",
        "tailwindcss": "^3.3.5"
    }
}
